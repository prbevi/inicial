/* Como queremos evitar que alguien vuelva a definir las variables que hemos creado y que nos haga perder la información que conseguimos,
vamos a hacer un cambio que, en parte, nos va a ayudar a evitarlo.

Vamos a repetir el mismo paso que antes, pero en lugar de var, ahora debemos usar let. */

let nombre;
let apellido;
let edad;
let telefono;
let sabeProgramar;